# ─────────────────────────────
# CORE APP INFO
# ─────────────────────────────
APP_NAME=Linkr
APP_ENV=development
APP_URL=http://localhost:3000

# ─────────────────────────────
# BACKEND / API
# ─────────────────────────────
API_PORT=4000
API_URL=http://localhost:4000
API_RATE_LIMIT_WINDOW_MS=60000
API_RATE_LIMIT_MAX=100

# JWT / AUTH
JWT_ACCESS_SECRET=changeme-access-secret
JWT_REFRESH_SECRET=changeme-refresh-secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# PASSWORD RESET
PASSWORD_RESET_TOKEN_TTL_MINUTES=30

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:19006

# ─────────────────────────────
# DATABASE (LOCAL)
# ─────────────────────────────
DATABASE_URL=postgresql://linkr:linkr@db:5432/linkr?schema=public

# ─────────────────────────────
# STORAGE (DIGITALOCEAN SPACES OR S3)
# ─────────────────────────────
STORAGE_DRIVER=spaces          # "spaces" | "s3" | "local"
STORAGE_BUCKET=linkr-media
STORAGE_ENDPOINT=https://linkr-media.sgp1.digitaloceanspaces.com
STORAGE_CDN_URL=https://cdn.linkr.co.nz
STORAGE_ACCESS_KEY_ID=changeme
STORAGE_SECRET_ACCESS_KEY=changeme

# ─────────────────────────────
# EMAIL (TRANSACTIONAL)
# ─────────────────────────────
EMAIL_FROM="Linkr Support <no-reply@linkr.co.nz>"
EMAIL_DRIVER=smtp              # or "resend" / "ses" later
EMAIL_SMTP_HOST=smtp.mailtrap.io
EMAIL_SMTP_PORT=2525
EMAIL_SMTP_USER=changeme
EMAIL_SMTP_PASS=changeme

# ─────────────────────────────
# KYC / JUMIO
# ─────────────────────────────
JUMIO_API_TOKEN=changeme
JUMIO_API_SECRET=changeme
JUMIO_BASE_URL=https://netverify.com
JUMIO_WEBHOOK_SECRET=changeme

# ─────────────────────────────
# PAYMENTS (SEGPay / CCBill)
# ─────────────────────────────
PAYMENTS_PRIMARY_GATEWAY=segpay      # "segpay" | "ccbill"
SEGWAY_MERCHANT_ID=changeme
SEGWAY_API_KEY=changeme
SEGWAY_WEBHOOK_SECRET=changeme

CCBILL_CLIENT_ACCOUNT=changeme
CCBILL_SUBACCOUNT=changeme
CCBILL_DATALINK_USERNAME=changeme
CCBILL_DATALINK_PASSWORD=changeme
CCBILL_WEBHOOK_SECRET=changeme

# ─────────────────────────────
# SEARCH / ANALYTICS (PHASE 1+)
# ─────────────────────────────
OPENSEARCH_URL=http://opensearch:9200
OPENSEARCH_USERNAME=admin
OPENSEARCH_PASSWORD=admin

POSTHOG_API_KEY=changeme
POSTHOG_HOST=http://localhost:8000

# ─────────────────────────────
# SAFETY CENTER
# ─────────────────────────────
SAFETY_ENCRYPTION_KEY=32-byte-hex-key-here-aaaaaaaaaaaaaaaaaaaaaaaaaaaa
SAFETY_DEFAULT_CHECKIN_MINUTES=60

# ─────────────────────────────
# FRONTEND (NEXT.JS)
# ─────────────────────────────
NEXT_PUBLIC_APP_NAME=Linkr
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_CDN_URL=https://cdn.linkr.co.nz
NEXT_PUBLIC_ENV=development

# ─────────────────────────────
# MOBILE (REACT NATIVE / EXPO)
# ─────────────────────────────
MOBILE_API_BASE_URL=http://10.0.2.2:4000   # Android emulator
MOBILE_SENTRY_DSN=
MOBILE_POSTHOG_API_KEY=

# ─────────────────────────────
# SECURITY / OPS
# ─────────────────────────────
ENCRYPTION_MASTER_KEY=32-byte-hex-key-here-bbbbbbbbbbbbbbbbbbbbbbbbbbbb
CLOUDFLARE_API_TOKEN=changeme
CLOUDFLARE_ACCOUNT_ID=changeme

# Admin bootstrap
INITIAL_ADMIN_EMAIL=founder@linkr.co.nz
INITIAL_ADMIN_PASSWORD=changeme-super-secure
